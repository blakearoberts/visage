---
name: auto-merge
description:
  Run the repository auto-merge script and report its output. Use when asked to
  enable auto-merge for the current pull request, wait for it to merge, and
  clean up the local branch.
---

# Auto-Merge

Run `bash .agents/skills/auto-merge/scripts/auto-merge.sh` from the repository
root and report the script output.
